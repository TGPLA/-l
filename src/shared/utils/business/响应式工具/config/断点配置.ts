// @审计已完成
/**
 * 响应式工具 - 断点配置
 * 
 * 定义响应式断点的像素值
 */

import type { Breakpoints } from '../types';

export const breakpoints: Breakpoints = {
  mobile: 640,
  tablet: 768,
  desktop: 1024,
  wide: 1280,
};
